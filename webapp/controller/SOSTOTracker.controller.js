sap.ui.define([
    "sap/ui/core/mvc/Controller"
], (Controller) => {
    "use strict";

    return Controller.extend("com.lipton.logisticshub.controller.SOSTOTracker", {
        /**
         * Initializes the SOSTOTracker controller.
         * @memberof com.lipton.logisticshub.controller.SOSTOTracker
         * @public
         * @returns {void}
         */
        onInit() {
        }
    });
});
